# ═══════════════════════════════════════════════════════════════════════════
#  FinanceLab / FinHubAI — ALL-PHASE Python Requirements
#  Covers Phases 1 → 8 in one install. Individual sections below map to the
#  plan-of-action phases. Grouped for clarity; pip installs them all.
# ═══════════════════════════════════════════════════════════════════════════

# ── Phase 1-2: Core API & Decision Engine ──────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
redis>=5.2.0
httpx>=0.28.0
requests>=2.32.0
aiohttp>=3.10.0
python-dotenv>=1.0.1
orjson>=3.10.0
tenacity>=9.0.0
cachetools>=5.5.0

# ── Market & Financial Data ────────────────────────────────────────────────
yfinance>=0.2.50
pandas>=2.2.0
numpy>=2.0.0
pandas-ta>=0.3.14b0
scipy>=1.14.0
pytz>=2024.2
python-dateutil>=2.9.0

# ── Scheduler / Workers ────────────────────────────────────────────────────
apscheduler>=3.10.4
celery>=5.4.0
kombu>=5.4.0

# ── Phase 3: AI / RAG / NLQ / Ollama ───────────────────────────────────────
ollama>=0.4.0
chromadb>=0.5.20
sentence-transformers>=3.3.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
tiktoken>=0.8.0
faiss-cpu>=1.9.0

# ── Phase 3-7: Sentiment / News / Scraping ────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.3.0
feedparser>=6.0.11
# newspaper3k is archived and often fails to build on Python 3.12+; skip by default.
# If you need article extraction, install manually: pip install newspaper4k
textblob>=0.18.0
vaderSentiment>=3.3.2
pytrends>=4.9.2

# ── Phase 4-7: ML / Factors / Backtesting ─────────────────────────────────
scikit-learn>=1.5.0
xgboost>=2.1.0
lightgbm>=4.5.0
statsmodels>=0.14.0
vectorbt>=0.26.0
quantstats>=0.0.64
# empyrical is abandoned and broken on Python 3.12 — use the maintained fork
empyrical-reloaded>=0.5.9
arch>=7.2.0

# ── Phase 6: Tax Engine & Export ──────────────────────────────────────────
openpyxl>=3.1.0
xlsxwriter>=3.2.0
reportlab>=4.2.0
pypdf>=5.1.0
python-docx>=1.1.0
babel>=2.16.0
forex-python>=1.8
pycountry>=24.6.1

# ── Phase 2-8: Alerts / Messaging / Notifications ─────────────────────────
python-telegram-bot>=21.7
discord-webhook>=1.3.1
slack-sdk>=3.33.0
twilio>=9.3.0
apprise>=1.9.0

# ── Phase 4-5: Cloud / Supabase / Storage ─────────────────────────────────
supabase>=2.10.0
boto3>=1.35.0
google-auth>=2.36.0

# ── Phase 8: NLP / Transformers (FinBERT etc.) ────────────────────────────
transformers>=4.46.0
tokenizers>=0.20.0
accelerate>=1.1.0
# torch is installed in setup.sh (platform-specific wheel)

# ── Phase 7: Corporate Actions / Options / Quant ──────────────────────────
# mibian is unmaintained and breaks on py3.12 — py_vollib covers the same math.
py_vollib>=1.0.1
quandl>=3.7.0

# ── Phase 5: Security / Auth / Hardening ──────────────────────────────────
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
itsdangerous>=2.2.0
cryptography>=43.0.0
pyjwt>=2.9.0

# ── Phase 5: Monitoring / Observability ───────────────────────────────────
sentry-sdk[fastapi]>=2.18.0
prometheus-client>=0.21.0
structlog>=24.4.0
rich>=13.9.0
loguru>=0.7.2

# ── Phase 8: Apple Ecosystem & Mobile Push (server-side) ──────────────────
aioapns>=3.2
pywebpush>=2.0.1

# ── Testing / Tooling ──────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
ruff>=0.8.0
mypy>=1.13.0
black>=24.10.0
